//////////////////// Block IPv6 AAAA query /////////////////////////
// It is recommended to place it before other modules
// 建议放在其他模块之前
modules = append(modules, {
    dnsquery: func(m) {
        if m.type == "AAAA" {
            return {"block": true}
        }
    }
})

